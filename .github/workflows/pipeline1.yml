name: Continuos Integration

on: pull_request

jobs: 
  continuos-integration:
    runs-on: ubuntu-latest

    steps: 
      - name: Checkout do código
        uses: actions/checkout@v4 

      - name: Validar Build do Docker
        run: docker build -t test-imagem .